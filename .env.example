# Proposed Stage 2 names. Templates only; no loader exists in Stage 1.
# Copy to ignored .env for local test setup; see docs/providers/credentials.md.
OPENEPW_NLR_API_KEY=
OPENEPW_NLR_EMAIL=
OPENEPW_CDS_KEY=
OPENEPW_OPENMETEO_API_KEY=
OPENEPW_CONFIG=config.local.toml
OPENEPW_DATA_DIR=.local/openepw

# Required only for remotely bound REST deployments
# OPENEPW_BEARER_TOKEN=
