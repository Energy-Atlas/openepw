# Copy to ignored config.local.toml. Programmatic > environment > local TOML.
[runtime]
data_root = ".local/openepw"
timeout = 60
retries = 2
max_response_bytes = 50000000
max_climate_bytes = 3000000000
workers = 2

[credentials]
# Prefer environment or explicit RuntimeConfig.load(env_file=".env").
# nlr_api_key = ""
# nlr_email = ""
# cds_key = ""
# openmeteo_api_key = ""
# bearer_token = ""
